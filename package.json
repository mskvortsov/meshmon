{
  "name": "meshmon",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run generate && vite build",
    "generate": "mkdir -p generated && pbjs -t json -p protobufs protobufs/meshtastic/*.proto -o generated/meshtastic.json",
    "preview": "vite preview"
  },
  "dependencies": {
    "crypto-js": "^4.2.0",
    "mqtt": "^5.5.2",
    "paho-mqtt": "^1.1.0",
    "protobufjs": "^7.2.6"
  },
  "devDependencies": {
    "protobufjs-cli": "^1.1.2",
    "vite": "^5.2.0"
  }
}
